# Seamap

A web map application for exploring marine spatial data, comprising a
ClojureScript frontend (shadow-cljs) and a Django backend.

## Getting started

The quickest way to get a local development environment running is with Docker —
a new machine only needs Docker installed, no local Python, Node, Java, or
database drivers.

See **[DOCKER.md](DOCKER.md)** for setup and usage.
